#ifndef HEADER_H
#define HEADER_H
#define 流れてく
#define 時の中ででも
#define 気だるさが
#define ほらグルグル廻って
#define 私から
#define 離れる心も
#define 見えないわ
#define そう知らない
#define 自分から if (get_datafiles_handled(&data, &files) == HANDLED_FAIL) { close_datafiles_handled(&files); return ERR_FILE_IO; };
#define 動くこともなく
#define 時の隙間に if (from_bmp_handled(&files, &img)) { close_datafiles_handled(&files); destroy_img_handled(&img); return ERR_FILE_FORMAT;};
#define 流され続けて if (transform_handled(&data, &img)) { close_datafiles_handled(&files); destroy_img_handled(&img); return ERR_TRANSFORM; };
#define 知らないわ if (to_bmp_handled(&files, &img)) { close_datafiles_handled(&files); destroy_img_handled(&img); return ERR_WRITE; };
#define 周りのことなど close_datafiles_handled(&files);
#define 私は私 destroy_img_handled(&img);
#define それだけ return ERR_OK;

#endif //HEADER_H
